{
  "private": true,
  "name": "private-discord-bot",
  "version": "1.0.0",
  "description": "A Cloudflare worker that performs the Oauth2 code grant based on the server id",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Merlintor <merlinfuchs2001@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
